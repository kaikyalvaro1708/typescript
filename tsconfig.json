// Configurando o ts
{
    "compilerOptions": {
        "outDir": "dist/js", //Compilado para dist/js
        "target": "ES6", //coverter para o EMC 6
        "noEmitOnError": true, //evitar que gera um js se tiver erro
    },
    "include":["app/**/*"] //onde está o ts
}